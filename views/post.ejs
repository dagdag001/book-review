<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= response.book_name %></title>
    <link rel="stylesheet" href="/styles/main.css">
    <style>
        .container {
            max-width: 800px;
            margin: 0 auto;
            padding: 20px;
        }
        .cover {
            float: left;
            margin-right: 20px;
        }
        .response {
            margin-top: 20px;
        }
    </style>
</head>
<body>
    <%- include('partials/header.ejs') %>
    <div class="container">
        <h1><%= response.book_name %></h1>
        <div class="cover">
            <img src="<%= response.cover %>" alt="Book Cover" width="200" height="200">
        </div>
        <div class="response">
            <p><%= response.review %></p>
            <small>Posted on: <%= response.post_date %></small>
        </div>
    </div>
    <%- include('partials/footer.ejs') %>

</body>
</html>